fix(deploy): 健康检查改用 127.0.0.1，修掉「假 unhealthy」

容器内 `localhost` 会先解析到 IPv6 `::1`，而 nginx 默认只监听 IPv4，
于是 `wget http://localhost/health` 直接 Connection refused。

后果不是「服务挂了」，而是更麻烦的一种：**站点完全是好的，
但 docker ps 一直报 unhealthy**，FailingStreak 涨到 20。
这类「监控说挂了而服务没挂」比真挂更费排查时间 ——
因为它会让人先去查服务，而问题在健康检查本身。

后端那处也一起改成 127.0.0.1：curl 会自动回退到 IPv4 所以一直是通的，
但「依赖客户端恰好会回退」不该是一个健康检查的前提。
